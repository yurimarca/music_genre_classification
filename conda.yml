name: music_genre_classification
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - requests
  - pip
  - mlflow
  - hydra-core
  - pip:
      - protobuf==3.20
      - wandb
      - hydra-joblib-launcher
      - databricks_cli
